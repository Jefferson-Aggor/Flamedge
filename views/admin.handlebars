<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FLAMEDGE | Adminstration</title>
</head>
<style>
    .no-booking {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 60vh;
        width: 60vw;
    }
</style>

<body>

    {{> _admin-showcase}}
    <div class="container">

        <div class="my-2">
            {{#if book}}
            <h1 class="text-grey">Books Made / {{book.length}}</h1>
            <div class="grid-4">
                <div class="divider-lg"></div>
            </div>
            <div class="grid-3">
                {{#each book}}
                <div class="my-1 p-1  card hoverable-sm">
                    <div class="name-date">


                        <p class="uppercase"><i class="fas fa-user"></i>
                            <strong style="font-weight: 600;color:red">{{firstname}} {{lastname}}</strong>
                        </p>
                        <div class="divider"></div>
                        <p>Book Date : {{formatDate date 'Do MMMM  YYYY, h:mm a'}}</p>
                    </div>
                    <div class="details">
                        <p><i class="fas fa-phone"></i> <a href="tel:{{contact}}">{{contact}}</a></p>
                        <p><i class="fas fa-envelope"> <a href="mailto:{{email}}">{{email}} </a></i></p>
                    </div>
                    <div class="work">
                        <p class="my uppercase">Work Type: <strong>{{work}}</strong> </p>
                        {{#if expectations}}
                        <p> Expectations: <br> {{expectations}}</p>
                        {{else}}
                        <p> Expectations: <strong>None!</strong></p>
                        {{/if}}
                    </div>
                </div>
                {{/each}}
                {{else}}
                <div class="no-booking">
                    <h3>No Bookings yet..</h3>
                </div>

            </div>
            {{/if}}
        </div>
    </div>
</body>

</html>